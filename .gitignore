node_modules/
dist/
*.log
.env
.env.local
secrets.env
data/
.sessions/
# playwright-cli drops snapshots and screenshots here when a run has no
# configured output directory of its own.
.playwright-cli/
.playwright/

# macOS writes these into every directory it opens; none of them belong to the repository.
.DS_Store
