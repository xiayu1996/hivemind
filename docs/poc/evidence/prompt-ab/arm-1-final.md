Mr.Ryan 已修复失败用例：折扣改为先从小计扣除，再计算税额（`src/cart.js:9-12`）。

验证已执行 `npm test`：
```text
ℹ pass 2
ℹ fail 0
```

`applyCoupon` 未实现：当前仓库没有其函数签名、优惠券数据结构或叠加规则。需要明确优惠券类型（固定金额/百分比）、叠加顺序、是否允许负数及税前/税后应用规则，否则会导致不同实现结果。